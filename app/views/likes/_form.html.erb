<%= form_with(model: like) do |form| %>
  <% if like.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(like.errors.count, "error") %> prohibited this like from being saved:</h2>

      <ul>
        <% like.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div hidden>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div hidden>
    <%= form.label :tweet_id, style: "display: block" %>
    <%= form.text_field :tweet_id %>
  </div>

  <%#= link_to edit_tweet_path(tweet) do%>
  <%#= link_to 'submit', :onclick => "$('#form_id').submit()" do %>
    <%#= image_tag("like_icon.png") %>
  

  <%#= form.submit "", :type => :image, :src => "app/assets/images/like_icon.png" %>
  <div id=""><p>
    <%= image_submit_tag("like_icon.png") %></p>
    <%=  %>
  </div>

<% end %>
